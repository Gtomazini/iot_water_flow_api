version: '3.8'

services:
  watergame-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - keys.env
    volumes:
      - .:/app
    restart: unless-stopped
    container_name: watergame-api